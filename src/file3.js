let numbers = [1, 2, 3, 4, 5];

// Accessing elements
let firstElement = numbers[0];

// Modifying elements
numbers[2] = 10;

// Array length
let arrayLength = numbers.length;

// Adding elements
numbers.push(6);

// Removing elements
numbers.pop();
